let circleRadius = 5;

let circleArea = Math.PI * Math.pow(circleRadius, 2); 
let circumference = 2 * Math.PI * circleRadius; 

console.log(`The area is approximately ${circleArea.toFixed(2)} square units.`);

console.log(`The circumference is approximately ${circumference.toFixed(2)} units.`); 
